{
  "name": "howtostartavegetablegarden",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name howtostartavegetablegarden --branch main",
    "postinstall": "git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.14",
    "astro": "^5.6.1",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "playwright-core": "1.58"
  }
}
